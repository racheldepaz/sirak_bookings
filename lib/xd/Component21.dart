import 'package:flutter/material.dart';

class Component21 extends StatelessWidget {
  const Component21({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 72.0,
      height: 72.0,
      decoration: BoxDecoration(
        border: Border.all(
          width: 7.0,
          color: const Color(0xFFC2291D),
        ),
      ),
    );
  }
}
